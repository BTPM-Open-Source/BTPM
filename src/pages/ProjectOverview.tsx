/**
 * DEPRECATED — NOT ROUTED.
 *
 * The live Project Overview surface is `src/pages/ProjectOverviewTab.tsx`,
 * mounted via `ProjectLayout` under `/workspace/:workspaceId/project/:projectId`.
 *
 * This file is retained only as a reference scaffold and is intentionally not
 * exported from the router. Do not add new features here — edit
 * `ProjectOverviewTab.tsx` instead. Send Email (Step 5.3) and Files (Step 5.4)
 * live on that surface.
 */
export default function ProjectOverviewDeprecated() {
  return null;
}
